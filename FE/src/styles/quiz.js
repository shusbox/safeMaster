import styled from "@emotion/styled";

const NumberStyled = styled.div`
  width: 20px;
  height: 20px;
`;

const NumberTextStyled = styled.p`

`;

const ResponseStyled = styled.div`
  display: flex;
  flex-direction: row;
`

export { NumberStyled, NumberTextStyled, ResponseStyled };